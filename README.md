# DEAD SQUAD (it's a lorojongrang project....)
Based on Laravel 8.0

# [Requirement]
* Install PHP version >= 7.4
* Install MySQL
* Install Apache/NGinX
* Create VHost

# [Initial Start]
* Copy file ".env.example" to ".env"
* Run Command
  ```sh
    $ composer update
    $ php artisan key:generate
